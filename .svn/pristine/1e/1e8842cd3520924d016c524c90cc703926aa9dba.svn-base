from django.urls import path

from taskmanage import views

urlpatterns = [
    path(r'taskManage/', views.taskManage),
    path(r'addTask/', views.addTask),
    path(r'recvTask/', views.recvTask),
    path(r'startTask/', views.startTask),
    path(r'gettaskname/', views.gettaskname),

]

# urlpatterns = format_suffix_patterns(urlpatterns)
